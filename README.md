NNTools -- package for data preprocessing and neural network training.
======

Check the README files of the [preprocessing](preprocessing) module and the [training](training) module for instructions. 